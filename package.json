{
  "private": true,
  "scripts": {
    "prepare": "husky install",
    "build": "lerna run build",
    "lint": "lerna run lint",
    "format": "lerna run format",
    "test": "lerna run test"
  },
  "packageManager": "pnpm@8.6.0",
  "devDependencies": {
    "husky": "^8.0.3",
    "lerna": "^6.6.2",
    "lint-staged": "^13.2.2"
  },
  "lint-staged": {
    "apps/earthquake/**/*.{ts,vue}": [
      "prettier --write --config \"./apps/earthquake/.prettierrc\" \"src/**/*.ts\" \"test/**/*.ts\" ",
      "eslint \"{src,apps,libs,test}/**/*.ts\" --fix -c \"./apps/earthquake/.eslintrc.js\""
    ]
  }
}
