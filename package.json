{
  "private": true,
  "scripts": {
    "prepare": "husky install",
    "build": "lerna run build",
    "lint": "lerna run lint",
    "format": "lerna run format",
    "test": "lerna run test"
  },
  "packageManager": "pnpm@8.6.0",
  "devDependencies": {
    "husky": "^8.0.3",
    "lerna": "^6.6.2"
  }
}
